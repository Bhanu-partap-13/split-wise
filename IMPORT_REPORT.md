# Import Report — expenses_export.csv

> **File**: `expenses_export.csv`
> **Detected**: 18 columns · 42 rows
> **Generated by**: SplitSmart CSV Import Wizard (Step 2 — Validate)

---

## Summary

| Metric | Value |
|---|---|
| Total Rows | 42 |
| ✅ Clean | 25 |
| ⚠️ Warnings | 16 |
| ❌ Skipped | 2 |
| **Rows to Import** | **40** |

---

## Name Variants Detected

| Variant | Note |
|---|---|
| `Rohan` / `rohan` | Normalise in your file if they are the same person |
| `Priya` / `priya` | Normalise in your file if they are the same person |

---

## Multi-Currency Detected

Currencies found: **INR, USD**

---

## Conflicts & Warnings (18)

| Row | Issue Code | Description | Action |
|---|---|---|---|
| 13 | `MISSING_PAYER` | No payer specified (`paid_by` is empty — *"can't remember who paid"*) | ❌ **Skipped** |
| 14 | `IS_SETTLEMENT` | *"Rohan paid Aisha back"* — looks like a settlement/payment transaction | ⚠️ Imported · `isSettlement = true` |
| 15 | `PERCENTAGE_MISMATCH` | *"Pizza Friday"* — percentages sum to **110.0%**, not 100% | ⚠️ Imported · amounts may be off |
| 16 | `AMBIGUOUS_DATE` | Date `"01/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 17 | `AMBIGUOUS_DATE` | Date `"03/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 18 | `AMBIGUOUS_DATE` | Date `"05/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 19 | `AMBIGUOUS_DATE` | Date `"08/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 20 | `AMBIGUOUS_DATE` | Date `"09/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 21 | `AMBIGUOUS_DATE` | Date `"10/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 22 | `AMBIGUOUS_DATE` | Date `"10/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 23 | `AMBIGUOUS_DATE` | Date `"11/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 24 | `AMBIGUOUS_DATE` | Date `"11/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 25 | `AMBIGUOUS_DATE` | Date `"11/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 26 | `AMBIGUOUS_DATE` | Date `"12/03/2026"` is ambiguous (DD/MM or MM/DD?) | ⚠️ Imported · assumed DD/MM |
| 26 | `NEGATIVE_AMOUNT` | *"Parasailing refund"* — negative amount (`-30`) treated as refund/credit | ⚠️ Imported · stored as refund |
| 31 | `ZERO_AMOUNT` | *"Dinner order Swiggy"* — amount is `0` | ❌ **Skipped** |
| 32 | `PERCENTAGE_MISMATCH` | *"Weekend brunch"* — percentages sum to **110.0%**, not 100% | ⚠️ Imported · amounts may be off |
| 34 | `AMBIGUOUS_DATE` | Date `"04/05/2026"` is ambiguous (DD/MM or MM/DD?) — note in CSV: *"is this April 5 or May 4? format is a mess"* | ⚠️ Imported · assumed DD/MM |

---

## Skipped Rows — Detail

### Row 13 — `MISSING_PAYER`
```
Expense     : House cleaning supplies
Date        : 2026-02-22
Amount      : ₹780
Paid By     : [EMPTY]
Notes       : "can't remember who paid"
Action      : SKIPPED — cannot calculate splits without a payer
```

### Row 31 — `ZERO_AMOUNT`
```
Expense     : Dinner order Swiggy
Date        : 22/03/2026
Paid By     : Priya
Amount      : 0
Notes       : "counted twice earlier - fixing later"
Action      : SKIPPED — zero-amount rows create ₹0 debt entries
```

---

## Final Import Action

```
Import 40 rows  (skip 2 errors)
```

The 40 accepted rows (25 clean + 15 warning rows\*) were written to the database via `expenses.importBatch`.

> \* Row 26 has two warnings (AMBIGUOUS_DATE + NEGATIVE_AMOUNT) but counts as a single row imported.

Each imported row was stored with:
- `importBatchId` — links all rows to this import session for audit
- `isSettlement = true` — set on Row 14 (*"Rohan paid Aisha back"*)
- `paidByName` — used for payers not matched to registered accounts (e.g., `Dev`, `Sam`, `Kabir`)
